# Castalia 目录整理说明

日期：2026-09-10

只整理了 `D:\AI` 里和 Castalia 相关的目录。LobeHub、ComfyUI、Lemonade、归档备份等未动。

## 为什么整理

原先 5 个相关目录散落在 `D:\AI` 根下，名字也不统一：

| 旧路径 | 实际身份 |
|---|---|
| `D:\AI\castalia-lite` | Castalia 中立核心（Lite）Git 库 |
| `D:\AI\ai-memory` | Castalia-Full Git 库 |
| `D:\AI\AI memory` | Castalia-Anima Git 库（名字带空格） |
| `D:\AI\castalia-run` | Lite 运行实例，无 Git |
| `D:\AI\anima-run` | Anima 运行实例，无 Git |

## 现在的结构

全部收到 `D:\AI\castalia\`：

```
D:\AI\castalia\
  Castalia\              ← 核心源码（本说明所在目录）
                           git@github.com:ehwin/Castalia.git
  Castalia-Full\         ← 联邦版源码
                           origin: ehwin/Castalia
                           full:   ehwin/Castalia-Full
  Castalia-Anima\        ← 情感版源码
                           git@github.com:ehwin/Castalia-Anima.git
  run\
    Castalia\            ← Hermes 用的运行实例（原 castalia-run）
    Castalia-Anima\      ← AIRI 用的运行实例（原 anima-run）
```

路径对照：

| 旧 | 新 |
|---|---|
| `D:\AI\castalia-lite` | `D:\AI\castalia\Castalia` |
| `D:\AI\ai-memory` | `D:\AI\castalia\Castalia-Full` |
| `D:\AI\AI memory` | `D:\AI\castalia\Castalia-Anima` |
| `D:\AI\castalia-run` | `D:\AI\castalia\run\Castalia` |
| `D:\AI\anima-run` | `D:\AI\castalia\run\Castalia-Anima` |

同盘移动，Git 历史和 `node_modules` 都还在。没有做 junction，旧路径已不存在。

## 跟着改过的引用

为避免 AIRI / 星图 / 托盘找不到记忆库，改了这些活配置（历史任务报告 `_dsh_task*` 没改）：

AIRI：

- `D:\system\AIRI\scripts\start-airi.bat`
- `D:\system\AIRI\scripts\airi_tray.py`
- `D:\system\AIRI\unified-proxy\config.py`
- `D:\system\AIRI\viz\routes\lib.js`
- `D:\system\AIRI\memory\scripts\migrate_to_anima.py`

Castalia 源码与运行实例：

- `Castalia` / `Castalia-Full` 的 `scripts\migrate_memdir.py`、`sync_libs_feature.py`、`sync_memdir_patch.py`、`sync_reflect_all.py`、`start-embed.bat`
- `Castalia` / `Castalia-Full` / `run\Castalia` 的 `web` 或 `viz\routes\lib.js`
- `run\Castalia\scripts\castalia_tray.py`
- `run\Castalia-Anima\scripts\verify_anima_keys.py`、`start-embed.bat`
- `Castalia-Anima\scripts\start-embed.bat`

LobeHub 运行实例里指向 Castalia 记忆目录的两处（否则星图聚合会断）：

- `D:\AI\lobehub-run\viz\routes\lib.js`
- `D:\AI\lobehub-run\scripts\castalia_tray.py`

## 没动的

- `D:\AI\lobehub`、`D:\AI\lobehub-run` 本身（只改了其中指向 Castalia 的路径）
- `D:\system\AIRI` 安装目录和 `D:\system\AIRI\memory`（AIRI 自带的 Anima 源码仍在那里）
- `D:\AI\_归档`、`_sync_bak_20260824` 等备份
- `Castalia-Full` 里的 `_dsh_task*` 历史报告（里面仍写旧路径，仅作当时记录）

## 使用提示

- 改 Castalia 核心代码：`D:\AI\castalia\Castalia`
- 改联邦版：`D:\AI\castalia\Castalia-Full`
- 改情感版源码：`D:\AI\castalia\Castalia-Anima`
- AIRI 实际跑的记忆体：`D:\AI\castalia\run\Castalia-Anima`
- Hermes 实际跑的记忆体：`D:\AI\castalia\run\Castalia`

若托盘或 AIRI 启动器是整理前就开着的，需要重启一次才会用上新路径。

---

## 后续改动（同日）

目录整理之后，又按实际使用方式改了托盘和星图。逻辑如下。

### 记忆怎么分

各软件**自己写自己的库**，物理隔离：

| 软件 | 引擎 | 记忆目录 | 写入 |
|---|---|---|---|
| Hermes | Full 类（`run\Castalia`） | `run\Castalia\memory` | 只写 Hermes 盘 |
| LobeHub | Full 类（`lobehub-run`） | `lobehub-run\memory` | 只写 LobeHub / 术数盘 |
| AIRI | **Anima**（`run\Castalia-Anima`） | `run\Castalia-Anima\memory` | 只写 `airi/` |

**AIRI 是特例**：对话进程带 `FEDERATION_DIRS` 且 `MCP_TOOLS=all`，可以用 `memory_search_all` **只读** Hermes / LobeHub / 自己。写入仍只进 Anima。

Hermes 对话默认没有跨库工具。LobeHub 记忆桥已去掉 `FEDERATION_DIRS`，不再从桥上读联邦。

联邦总反思 `reflect_all` 只读各源库，洞察写进 `reflect` 总库，不改源库。

三个代码版本：Lite = 单实例核（当前 `src` 无联邦工具）；Full = 中立核 + `memory_search_all` / `reflect_all`；Anima = 情感层 + 联邦，默认人格 `airi`。

### 托盘：只留一份

不再单独做 AIRI 托盘。`airi_tray.py` 和 `lobehub-run\scripts\castalia_tray.py` 都跳到正本：

`D:\AI\castalia\run\Castalia\scripts\castalia_tray.py`

`D:\system\AIRI\scripts\启动托盘.bat` 也启动这一份。图标是青绿星标。

| 操作 | 行为 |
|---|---|
| 左键 | 打开**联邦总图** `http://127.0.0.1:3345/?scope=federation` |
| 打开 → 联邦总图 | 多星系总图（Hermes / AIRI / Reflect 等） |
| 打开 → 记忆总成 | 列表/跨库搜索页 `/aggregate.html` |
| 打开 → Hermes / LobeHub / AIRI 星图 | **只画该端自己的库** |
| 打开 → AIRI / Admin / LobeHub | 应用本身 |
| 服务 | 嵌入、三端星图、记忆桥、AIRI、AIRI 代理、LobeHub 开关 |
| 退出 | 停嵌入 / 星图 / 桥，**不关** AIRI 窗口 |

其它托盘改动：左键默认打开；服务是开关不是成对启停；点了有通知；端口按精确端口号判断；LobeHub 启停进后台线程，不再卡死菜单。

AIRI 代理默认嵌入改为 Castalia 的 `:11436`（不再另起 `:11435`）。

### 前端：两套图拆开

原先三端星图的 `/api/graph` 都扫全部 `AGGREGATE_DIRS`，AIRI 自己的图和联邦总图糊成一张；顶栏还有「总成视图」和「记忆总成」两个入口。

已改：

- `/api/graph?scope=local`（默认）：只画当前实例
- `/api/graph?scope=federation`：联邦总图
- 实例页标题变成 Hermes / LobeHub / AIRI，链到联邦总图
- 联邦总图页标题「Castalia 联邦总图」，不再显示那条链接
- 去掉「总成视图」按钮（和总图重复）

改过的前端文件：

- 源码：`Castalia-Full\web\routes\graph.js`、`Castalia-Full\web\public\index.html`
- 活体：`run\Castalia\viz\`、`D:\AI\lobehub-run\viz\`、`D:\system\AIRI\viz\` 里对应的 `graph.js` / `index.html`

星图进程要重启一次（托盘里把「星图」关再开）才会吃到 `graph.js`。

### 星图视觉吸收（同日）

壳还是旧 GitHub 深色，Bloom 没开，所以看起来「总不好看」。对照了几份观感更好的开源星图后，把能直接吃进现有 3d-force-graph 的部分落地了（没换引擎）：

| 来源 | 吸收了什么 |
|---|---|
| [Sereo-430/memory-starmap](https://github.com/Sereo-430/memory-starmap) | Liquid Glass 面板、additive 光晕、重要节点更亮 |
| [cdeust/memory-monitor](https://github.com/cdeust/memory-monitor) | 景深雾、连线粒子加粗、空闲慢旋（8s 后恢复） |
| [VrtxOmega/drift](https://github.com/VrtxOmega/drift) | 深空底 `#07090D`、多层星尘、暗角 vignette |
| 本仓库 `_ui-redesign-mock` | 青绿强调色、圆角搜索、图例玻璃 |

改过的文件：三端活体 `viz/public/index.html` + Full `web/public/index.html`。刷新星图页即可（静态 HTML，不必重启 node）。未做：换 UnrealBloomPass（vendor 里没有这个 pass）、流星雨、抽成独立前端包。

### 五库前端对照（booboo / CerebroCortex / Graphiti / mnemory / codebase-memory）

源码在 `D:\AI\castalia\_refs-viz\`。只看前端，不搬他们的存储。

| 库 | 前端实际是什么 | 对 Castalia 有用吗 |
|---|---|---|
| **[booboo](https://github.com/jessymariau/booboo)** | 重型：百万节点点云 + 自定义 shader，**分层环 + 根节点**，设计 token（`#06080e` 底、奶油字、金/青）。核心法则：**最亮的像素必须是最重要的东西**（flag > 地标节点 > 普通点 > 结构边 > 背景）。 | **最该学。** 不是学它的 organigram，是学亮度层级。 |
| **[CerebroCortex](https://github.com/buckster123/CerebroCortex)** | 炼金术仪表盘：左 68px 图标轨、金 `#D4AF37`、类型 KPI 卡（Affective/Episodic…）。3D 图只是其中一块，ForceGraph3D 默认球，链接半透明金。 | 学壳：侧栏 + 类型卡；别学 Cinzel 衬线宗教风。 |
| **[Graphiti_Awesome_Memory](https://github.com/GoGoButters/Graphiti_Awesome_Memory)** | React Admin + `GraphViewer3D`：按度数上色（红黄绿蓝枢纽），**每个节点都挂 SpriteText**，黑底白边，粒子常开。 | 度数上色可参考枢纽；全员标签在 400+ 节点会糊，Castalia 继续「悬停才出字」。 |
| **[mnemory](https://github.com/fpytloun/mnemory)** | **管理后台**：Dashboard / Search / Memories / **Graph 是一个 Tab**。图是 **2D D3**，重要性→半径，pinned 青环，类型复选过滤。 | 学信息架构：图不是整站。类型筛选我们已经用图例点选做了。 |
| **[codebase-memory-mcp](https://github.com/DeusData/codebase-memory-mcp)** | 代码知识图谱 3D UI（函数/调用边），不是对话记忆。 | 不吸收数据模型；只要「图是内嵌功能、不是整套产品」。 |

**不要搬：** booboo 的百万点云引擎、Cerebro 的海马体/做梦隐喻、Graphiti 的 Neo4j、mnemory 的 Qdrant、codebase 的 tree-sitter。Castalia 仍是 SQLite memdir。

**已接到现图：** 普通节点光晕压暗（booboo 亮度法则），选中/悬停才亮；`/` 聚焦搜索。刷新即可。

### DeepSeek 借鉴对照（你点名的 MCP 3D 仓库）

对照了源码，**DeepSeek 那份 `_viz_plan` 有几处抄错了方向**：

1. **「固定球壳是对的」是错的。** SethRose / memory-monitor 都是力导向或云团聚类。把节点钉在薄球壳上，才会像空心足球。Castalia 不该学「锁死球壳」，该学「同类型聚在一起、重要的靠里」。
2. **「数据层抄 supermemory / memory.json」是错的。** SethRose 吃的是 MCP Knowledge Graph 的 entity/relation/observation；memory-monitor 吃的是 Claude Code 的 `~/.claude` markdown；MCP-Visualizer 吃 Neo4j。Castalia 是 SQLite memdir（user/feedback/project…）。把版本链、Neo4j、entity 模型塞进星图，会把可视化绑死在别人的数据模型上。
3. **「3d-force-graph 原生 Bloom」是错的。** vendor 里只有 EffectComposer，没有 UnrealBloomPass。memory-monitor 是自己上的 Three 后期管线，不是开关。
4. **ContextOS 因果边色**大半套不到现网：Castalia 大量边是 similarity 和合成的星系桥，不是 causes/leads_to。
5. **2D 仓库**（mjherich / dzivkovi / srijanshukla18）只适合筛选项和点选细节，不要当 3D 视觉来源。

**真正该吸收、已经接到现图上的：**

| 来源 | 吸收 |
|---|---|
| SethRose Memory-Knowledge-Graph-3D | 点选才让关系边跑粒子（默认安静）；空白处点击复位相机；按类型/项目筛选 |
| memory-monitor | 类型筛选条（做成左下图例可点）；Esc 关闭详情；粒子不要铺满全图 |
| 不吸收 | Neo4j、memory.json 实体模型、Claude markdown 扫描、2D D3 图 |

用法：左下图例点「用户画像」只看那一类；再点取消。点空白处相机拉远。Esc 清筛选并关面板。

记忆怎么铺（「散步」）：

原先钉在薄球壳上，看起来像一颗空心足球，导览还是 60 秒乱跳。现改为：

- **项目**：一个居中；两个左右分开；更多按体量拉开，避免叠在一起。
- **类型**：绕项目核心成花瓣环（user / feedback / project…），不再套小球壳。
- **单条记忆**：云团散布，重要的靠近类型核心，其余往外走（借鉴 Stellar Memory「重要的绕得近」）。
- **idle**：记忆绕自己的小核心慢慢转。
- **自动导览**：改成散步——优先沿关系边走，没有边就同类型、再同项目；9 秒一步，不再 60 秒随机传送。

---

## 架构 debug（同日）

对三套源码、三端运行实例、托盘和星图对了一遍。P0/P1 已按下面顺序落地；P2 的抽 core 包仍未做。

### 先分清「改哪份才会生效」

| 角色 | Git 源码 | 真正在跑的 |
|---|---|---|
| Hermes 记忆引擎 | 名义上 Lite，实际 `run\Castalia\dist` 是 **Full**（19 个模块，含 federation / reflectAll） | `D:\AI\castalia\run\Castalia` |
| AIRI 记忆引擎 | Anima | `D:\AI\castalia\run\Castalia-Anima\dist` |
| LobeHub 记忆引擎 | 无独立 Git | `D:\AI\lobehub-run\dist` |
| 星图 UI | Full `web\` 已改 scope | 活体有 **三份拷贝**：`run\Castalia\viz`、`lobehub-run\viz`、`AIRI\viz` |

Lite 的 `src` 没有 `federation.ts` / `reflectAll.ts`，但 Hermes 跑的不是这份。改 `Castalia\` Git 库**不会**自动进 Hermes。三端星图又是拷贝，改 Full `web\` 必须再同步到三个 `viz\`，否则会再漂。

**优化方向**：Git 只保留 Full + Anima；Hermes 从 Full 构建；星图做成一份静态资源，三端只换 `MEMORY_DB_DIR` / 端口，不要五份 `index.html`。

### P0 — 会错、会双开、会指错引擎

1. **两条启动链路、两套嵌入端口**  
   托盘把 AIRI 代理指到 `:11436`。`start-airi.bat` 仍自己拉 `:11435`，`unified-proxy\config.py` 默认也是 `11435`。不用托盘、用旧 bat 启动，AIRI 和 Hermes 各烧一份 Yuan 模型，或代理连不上托盘那份嵌入。  
   **优化**：只保留 11436；`start-airi.bat` 改成只启动托盘（或只启动 airi.exe，嵌入交给托盘）；`config.py` 默认改 11436。

2. **AIRI 星图里的反思调不到 Anima**  
   `AIRI\viz\routes\lib.js`：`ROOT = D:\system\AIRI`，`MCP_SERVER = D:\system\AIRI\dist\index.js`，这个文件**不存在**。图能画（读的是 `MEMORY_DB_DIR`→Anima），但管理页/反思会 spawn 错进程。  
   **优化**：`MCP_SERVER` 指向 `run\Castalia-Anima\dist\index.js`，或用环境变量覆盖。

3. **`reflect_all` 的 REFLECT_DIR 写成旧库文件**  
   现网记忆已是 memdir：`memory/<project>/<memType>/memory.sqlite`。`reflectAll.ts` 在设了 `REFLECT_DIR` 时却写 `REFLECT_DIR/project-reflect.sqlite`（老单文件）。托盘又给三端 viz 都配了 `REFLECT_DIR=lobehub-run\memory`。总反思结果可能写进星图扫不到的文件。  
   **优化**：REFLECT_DIR 只表示「总库所在实例的 memory 根」，写入走 `DatabaseManager.getInstance('reflect', memType)`。

### P1 — 联邦图会脏、职责会糊

4. **LobeHub 盘里有别人的库**  
   `lobehub-run\memory\` 除了 `lobehub/`、`shushu/`、`reflect/`、`shared/`，还有 **`airi/`、`hermes/`**。联邦总图按目录扫描，会把它们标成「LobeHub 的星系」，和 Hermes/AIRI 真库并排。多半是历史拷贝。  
   **优化**：确认无唯一数据后迁走或删除；扫描时只认本实例该有的 project 名。

5. **Hermes「自己的星图」其实是整盘**  
   `scope=local` 过滤的是实例目录，不是 `hermes/` 这一个 project。Hermes 盘上还有 `default/`、`shared/`、`reflect/`，本地星图会一起画。  
   不一定是 bug，但和「只画 Hermes」的说法不完全一致。若要严格隔离，local 再按 `CASTALIA_PROJECT` 滤一层。

6. **配置通道有两套**  
   引擎联邦看 `FEDERATION_DIRS`；星图总成看 `lib.js` 里写死的 `AGGREGATE_DIRS`（可被 `AGGREGATE_DIRS` 环境变量覆盖，托盘却没设）。改托盘里的 FEDERATION 不会自动改星图扫描名单。  
   **优化**：星图只读 `FEDERATION_DIRS` / `AGGREGATE_DIRS` 其中一个，托盘只写一份 JSON。

7. **「只有 AIRI 读联邦」在控制台层仍可绕开**  
   对话链路已经收紧（LobeHub 桥去掉 FEDERATION_DIRS）。但三端 viz 的 `mcpCall` 默认 `MCP_TOOLS=all`，人在 Hermes/LobeHub 管理页仍能调 `memory_search_all` / `reflect_all`。若原则是「模型不能、人可以」，应写明；若原则是「只有 AIRI 进程能跨库」，viz 的 MCP 也要收。

### P2 — 重复与漂移

8. **星图 UI 五份**  
   Lite `web\`（`index.html` 仍是旧的，`graph.js` 还是 `libFiles()` 全量）、Full `web\`、三个活体 `viz\`。这次 scope 拆分没改 Lite `web\`。  
   **优化**：见上，一份前端。

9. **三仓库靠 `sync_*.py` 补丁对齐**  
   Full 改完要手工 sync 到 Anima。Anima 的 `index.ts` 注释写「以通用版 refs 为基底再叠情感层」，长期会分叉。  
   **优化**：共享 `castalia-core` 包，Anima 只保留 emotion / bias / agentState / userLearning。

10. **死代码**  
    顶栏「总成视图」按钮已删，`toggleView()` 还在三端 `index.html` 里。（`yuan_embed_server.py` 无参默认已改为 11436。）

11. **硬编码盘符**  
    托盘、`lib.js`、`config.py`、`start-airi.bat` 写满 `D:\AI\...`、`D:\system\AIRI`、`D:\system\New Folder\node.exe`。换机即挂。  
    **优化**：托盘旁边放一份 `castalia.paths.json`，所有入口读它。

### 已落地（按建议顺序）

1. **嵌入统一 11436**  
   `config.py` / `reflect-config.json` 默认改为 11436。`start-airi.bat` 复用已在跑的 11436，否则启动 `run\Castalia\yuan_embed_server.py`；顺手杀掉残留 11435。`stop.bat` 不再杀 11436（托盘共用）。`yuan_embed_server.py` 无参默认改为 11436。

2. **AIRI viz MCP 指向 Anima**  
   各份 `viz/routes/lib.js`：`ANIMA_DIR` / `CASTALIA_MCP` 优先。托盘给 AIRI 星图加了 `CASTALIA_MCP=run\Castalia-Anima\dist\index.js`。配置文件改从 `MEMORY_DB_DIR/config.json` 读。

3. **`reflect_all` 写 memdir**  
   Full / Anima 源码已改：`REFLECT_DIR` 当作 memory 根，写入 `reflect/<memType>/memory.sqlite`。已 `tsc` 并拷到 `run\Castalia\dist`、`run\Castalia-Anima\dist`、`lobehub-run\dist`。

4. **LobeHub 脏库**  
   `lobehub-run\memory\airi`、`hermes` 均为 0 条，已迁到 `D:\AI\lobehub-run\_bak_foreign_20260910`（不在 memory 扫描根下）。盘上现为 `lobehub` / `shushu` / `reflect` / `shared` / `receipts`。

5. **星图名单读 `FEDERATION_DIRS`**  
   `AGGREGATE_DIRS` 优先解析 `FEDERATION_DIRS`，其次 `AGGREGATE_DIRS`，最后才是写死的三端路径。托盘本来就在 viz 环境里塞了 `FEDERATION_DIRS`，总图与引擎现在走同一份名单。

6. **未做**  
   抽 `castalia-core` 包、消灭三份 viz 拷贝。Lite `web\routes\graph.js` 已补上 `scope=local|federation`，但 Lite `index.html` 仍是旧顶栏。`toggleView()` 死代码还在。Hermes local 星图仍会画出该盘上的 `default/shared/reflect`。viz 管理页仍是 `MCP_TOOLS=all`（人可以跨库，模型对话链路已收紧）。

核对过的活路径：`run\Castalia`、`run\Castalia-Anima`、`lobehub-run`、`D:\system\AIRI\viz`、`unified-proxy\config.py`、`start-airi.bat`、托盘正本。

星图 / 代理进程要重启一次才会吃到 `lib.js` 和 `reflectAll.js`。

---

## 停手存档（星图试验已跑偏）

同一会话后半段注意力散了，在 `index.html` 上连续打补丁（布局、LOD、镜头、点选、分层），**没有先对照官方示例做最小可复现**，属于破坏性修改。下面把「还能留」和「已经搞乱」分开，方便下一场对话重新分析，不要接着这份星图往上叠。

### 仍然有效、应保留

| 项 | 位置 |
|---|---|
| 目录归拢 | `D:\AI\castalia\` 下 Castalia / Full / Anima / run |
| 托盘一份 | `run\Castalia\scripts\castalia_tray.py`；AIRI `启动托盘.bat` 和 `airi_tray.py` 跳转到它 |
| 嵌入只走 11436 | `unified-proxy\config.py`、`reflect-config.json`、`start-airi.bat`、`yuan_embed_server.py` |
| AIRI 代理走 Anima | 托盘 `CASTALIA_MCP` / `ANIMA_DIR` |
| `reflect_all` 写 memdir | Full/Anima `src/reflectAll.ts` |
| LobeHub 脏库迁走 | `D:\AI\lobehub-run\_bak_foreign_20260910` |
| 星图 API `scope=local\|federation` | `web/routes/graph.js` + 节点带 `instance` |
| `FEDERATION_DIRS` 驱动扫描 | `web/routes/lib.js` |

无 Git 的活体（改了但不会进仓库）：`run\Castalia\viz\`、`lobehub-run\viz\`、`D:\system\AIRI\viz\`、AIRI 脚本。以 Full 仓库的 `web\` 为源码侧快照。

### 已搞乱、下一场应重做而不是接着改

活体入口：`D:\AI\castalia\run\Castalia\viz\public\index.html`（与 Full `web/public/index.html` 同步过）。

破坏点：

1. **布局尺度一度放到 4000 单位**，相机停在 ~800，主节点投出屏幕，点选全空。后加 `fitLayout(..., 240)` 缩回视野，但是叠在一堆半成品上。
2. **自定义 `nodeThreeObject` Group 干掉默认球**，3d-force-graph 射线拾取失效。后又改回默认球 + 屏幕坐标点选兜底。
3. **改成 OrbitControls 后 `controls.update()` 每帧掐掉 `cameraPosition` tween**。官方 `click-to-focus` 示例是 `new ForceGraph3D` + `cameraPosition(node*distRatio, node, 3000)`，中间绕路了。
4. **LOD / 语义缩放把记忆全藏**，只剩主节点；再改三层展开，默认不显示单条记忆。用户看到的和「星系可点」预期不一致。
5. **弹簧、球壳、花瓣、Vogel pack、fitLayout 叠了多轮**，没有单一布局模型。

无头浏览器结论（仅供下一场参考）：直接调 `window.flyTo` 相机会动；屏幕坐标点主节点后镜头也动过。**不能当成产品已修好。** 用户现场仍反馈飞镜头/亮度/重叠不行。

### 下一场建议

1. 新开对话。以 Full 的 `7d5ee0a`（任务9 星图）或更早稳定的 `index.html` 为底，**不要从当前试验稿继续打补丁**。
2. 官方最小飞镜头：https://github.com/vasturiano/3d-force-graph/blob/master/example/click-to-focus/index.html
3. 分层/不重叠：默认只画结构团 + 类型团；点团再展开叶子。对标 Open Knowledge Maps，不要一次丢 400 点。
4. 先 puppeteer 点选 + 相机位移回归，再谈视觉。
5. 克隆参考只读：`D:\AI\castalia\_refs-viz\`（SethRose、memory-monitor、booboo 等），不要把它们的数据模型写进 Castalia。

### 本轮 Git 提交范围（有仓库的）

- `Castalia`：`整理说明.md`、`web/routes/graph.js`、`web/routes/lib.js`、路径脚本
- `Castalia-Full`：`src/reflectAll.ts`、`dist/reflectAll.js`、`web/public/index.html`（试验稿）、`web/routes/graph.js`、`web/routes/lib.js`
- `Castalia-Anima`：`src/reflectAll.ts`、`dist/reflectAll.js`

不提交：`_dsh_task*`、`_ui-redesign-mock/node_modules`、Lite 的无关 `dist/*.js`。


